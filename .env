# Core configuration (used by the RAG app and init scripts)
CHROMA_HOST=chromadb
CHROMA_PORT=8000

# Embedding model — must be the same across init and query to avoid drift.
# Changing this requires a re-embedding of existing vectors (see scripts/re_embed.py).
EMBEDDING_MODEL=sentence-transformers/all-mpnet-base-v2